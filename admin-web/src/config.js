// Mismo interruptor y mismo motivo que cliente-app/constants/config.js: el restaurante puede cambiar
// su precio y el total mostrado dejaría de coincidir con lo que realmente se cobra. El despachador
// tampoco necesita verlo para asignar motorizados. El envío sí se muestra siempre (lo fija la empresa).
export const MOSTRAR_PRECIOS = false;
